## 二、接口设计

### 2.1 创建审核流程接口

1. 逻辑：根据审核类型查询产品流程表并创建审核流程
2. 入参: 审核唯一编码、审核类型、用户ID...

用于业务系统调用：如提交企业资料or提交订单资料

### 2.2 流程审核接口

1. 入参: 审核唯一编码、审核人员ID、审核结果、驳回资料等、当前审核流程ID、审核类型
2. 逻辑：
   1. 判断审核情况是通过还是拒绝，如果是拒绝则通过审核类型调用业务系统（用户中心or订单中心）审核接口进行拒绝操作。
   2. 如果是通过则判断当前审核流程是否是最后一个审核流程，如果是最后一个流程则通过审核类型调用业务系统（用户中心or订单中心）审核接口进行审核。
   3. 修改流程表里面当前流程流状态
   4. 在审核流程记录表里面添加记录

审核通过、审核拒绝时调用

### 2.3 流程列表查询接口

1. 入参:  产品编码、用户ID、开始时间和结束时间
2. 逻辑：
   1. 通过用户ID去查询用户所在的审核组，如果没有审核组则直接返还空数据
   2. 通过审核组和业务类型分页获取当前用户可以审核的流程审核表里面的数据
   3. 用业务类型获取业务系统的dubbo接口
   4. 通过dubbo接口和分页获取的审核唯一编码泛化调用业务系统，获取业务系统的列表数据
   5. 组装审核信息后返还给前端

通用的业务审核列表。只是展示需要审核的数据

### 2.4 流程审核查询

1. 入参: 审核唯一编码、业务类型

2. 逻辑: 通过审核唯一编码查询审核流程表和审核流程记录表并组装数据后返回给前端


用于详情页里面的处理流程查询

### 2.5 详情页的接口

1. 审核组的增删改

   1. 逻辑：新增、删除、修改审核组信息，流程有未审核通过的数据不能进行删除和修改
2. 用户查询接口

   1. 用于审核组修改和添加用户时使用

## 三、数据库表设计

### 3.1 审核组和用户关联表（flow_group_user）

### 3.2 审核表（flow_review）

### 3.3 审核与审核组关联（flow_review_group）

### 3.4 审核操作记录表（flow_review_operate）

### 3.5 审核规则表（flow_review_rule）

### 3.6 审核规则和组的关联表（flow_review_group）

