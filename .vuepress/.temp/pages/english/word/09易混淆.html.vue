<template><div><h3 id="while" tabindex="-1"><a class="header-anchor" href="#while" aria-hidden="true">#</a> while</h3>
<div class="language-json line-numbers-mode" data-ext="json"><pre v-pre class="language-json"><code><span class="token comment">// “While”一会儿、当...的时候：是一个连词，用于引导时间状语从句，表示“当……的时候”</span>
“I was studying while my sister was watching TV.”（我在学习，而我的妹妹在看电视。）
<span class="token comment">// “Which”那一个、那个、那些、这些：是一个疑问代词，用于询问特定的人或事物</span>
“Which book do you want to read?”（你想看哪本书？）
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="this" tabindex="-1"><a class="header-anchor" href="#this" aria-hidden="true">#</a> this</h3>
<div class="language-text line-numbers-mode" data-ext="text"><pre v-pre class="language-text"><code>this  // 这、这个
that  // 那、那个
these // 这些
those // 那些

thus // 因此
than // 比、对比；less than 少于、不到
there // 在那里，在那边
three //三、三个
then  // 然后、那么、当时
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></div></template>


